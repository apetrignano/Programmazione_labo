#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

int main(int argc, char* argv[]) {

    char maxc = CHAR_MAX;
    int maxi = INT_MAX;
    char minc = CAR_MIN;
    int mini = INT_MIN;

    


    return 0;
}